import { redirectListQuerySchema } from '@cms/shared'

/**
 * Shape `packages/shared` me hai (R8). Yahan koi `create`/`update` schema **nahi** hai —
 * Slice 3 me redirects sirf apne aap bante hain (slug badalne pe). Haath se banana aur
 * badalna Phase 4 ke manager ke saath aayega.
 */
export { redirectListQuerySchema }
