import { createRedirectSchema, redirectListQuerySchema, updateRedirectSchema } from '@cms/shared'

/** Shape `packages/shared` me hai (R8) — admin ka form bhi wahi schema chalata hai. */
export { createRedirectSchema, redirectListQuerySchema, updateRedirectSchema }
