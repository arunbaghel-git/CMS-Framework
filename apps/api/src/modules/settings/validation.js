import { updateIntegrationsSchema, updateSettingsSchema } from '@cms/shared'

/** Shape `packages/shared` me hai (R8) — admin ka form aur API ek hi schema pe. */
export { updateIntegrationsSchema, updateSettingsSchema }
